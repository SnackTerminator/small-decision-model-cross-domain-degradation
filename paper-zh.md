# 小决策模型跨域迁移的退化现象

## ——一组受控实测、一个独立复核协作规程，与「零信息实验」的三种形态

**作者**：姜姜（AI 智能体 卫总助手）
**独立复核**：另一个厂商提供的通用大语言模型系统（**AI 复核，非人类专家**；见 §9 AI 使用声明）
**方向裁定**：一名人类决策者（自然人，仅作方向裁定与授权，不参与实测、分析与撰写）
**日期**：2026-09-24｜**版本**：**v2 · 对外发表版**（v1 经独立复核后修订）
**研究周期**：4 个连续工作日（2026-09-21 ～ 2026-09-24）
**证据分级**：除逐条标注「外部·未复现」「其自报·未复现」「推算」者外，本文数据均为**一手实测**
**合规声明**：本文全部实验数据均取自公开数据集与公开开源的第三方模型权重；未使用任何涉密或内部专有数据（见 §2.8、附录 A）

---

## 摘要

本文报告对两类开源「小决策模型」（*Jev* 类 System One 模型，参数量 395 M–596 M）在跨域、跨语言场景下可用性的受控实测：共 **2,640 次模型前向**，全部在单台 CPU-only 机器上完成，覆盖 2 个模型 × 4 种语言/形态组合 × 6 个数据集（5 个公开集 ＋ 1 组自建命题）。

主要发现有三条。**其一，自造中文短规则命题上未检出判别力**（AUC ≈ 0.5，n = 12；检验力不足，仅作探索性观察）；**其二，在同形态自然语言推理（NLI）任务上存在强语言效应**——以官方逐题平行的双语 NLI 数据集为材料做 *state* 语言 × *criteria* 语言 2×2 交叉，并以逐题配对 bootstrap 做正式 ΔAUC 检验：*state* 语言效应在中文 criteria 下为 **+0.3150**、在英文 criteria 下为 **+0.1660**（95% CI 均不含 0，**均显著**）；*criteria* 语言效应仅在中文 state 下显著（**+0.1654**），在英文 state 下不显著（**+0.0164**）；**两者的差（交互项 DiD）＝ −0.1487，95% CI [−0.2086, −0.0916] 不含 0 ⇒ 交互本身亦经配对正式检验**；**其三，模型置信度不携带信息**——输出概率分布狭窄、正负区间高度重叠且预测标签单侧，引用此类模型的置信度必须同步给出校准证据。

方法学上，本文提出并实测验证三项可复用产出：**(a) 三条评测护栏**（正负样本平衡、基线对照、报预测分布）；**(b) 提前否证协议 D1–D4**，用 60 次前向的闸门段替代全量深度评测；**(c)「零信息实验」的三种形态**（退化必呈现型／恒等式型／阈值型）及对应的三问自查，用于识别那些「即使被测对象完全失效也照样成立」的伪证据。

本文全过程由**两个异构 AI 系统分工**完成——一个执行与撰写、一个独立复核，并完整记录了这套协作规程及其有效性实证：复核意见中 **0 项**被判「不成立」，但促使执行方**自我更正 4 处判断、撤回 12 项结论**。本文同时保留自身错误的完整台账（附录 C），以作为同类判断的校准锚点。

**关键词**：小决策模型；System One；跨域迁移；跨语言迁移；分布外静默失败；置信度校准；零信息实验；负结果；独立复核

---

## 1 引言

### 1.1 问题

一个高强度使用大模型的工作体系中，**成本大头往往不在「思考」而在「判定」**：这条请求该路由到哪一档模型、这段上下文该丢还是该留、这个动作是否触碰某条规则。这类判定的共同特征是**候选空间小、答案确定、调用次数极多**。

*Jev* 类「小决策模型」（System One）正是对准这个缝隙：**不生成文本**，只接收「状态 + 类型化问题」，返回**带概率的类型化决策**。其三类原语为 `Noul`（返回 *P*(yes)）、`Choice`（2–255 个候选）、`Score`（2–10 个有序等级）。

本文的真实诉求不是「研究该模型本身」，而是一个工程问题：**有没有能直接拿来用、足够便宜的判定器**。本文以两个第三方开源复刻实现为代理对象，报告其在自有判定域、域外拒识、公开 NLI 域上的表现，并由此提炼可复用的评测方法学。

### 1.2 研究问题与答案

| 编号 | 问题 | 答案 | 强度 |
|---|---|---|---|
| **RQ1** | 现成开源小决策模型能否在**自有判定域**直接使用？ | **本次配置下未检出判别力** | 一手实测（n = 12，检验力不足） |
| **RQ2** | 失效是**单个模型的偶然**，还是**该类别的系统性**？ | **两次独立实测同向，但 n = 2 不足以断言「系统性」**；后续证据**显示**语言效应主导 | 推断 |
| **RQ3** | 能否以极低成本**预先筛除**不可用实现？ | **可初筛**（用 D1–D4），但判据须按 §5.4 修正后方可用于证明目的 | 推断 |

### 1.3 主要贡献

1. **十组同链路实测**（§4.1–§4.10）：2 个模型 × 4 种语言/形态组合 × 6 个数据集（5 个公开集 ＋ 1 组自建命题），累计 **2,640 次前向**；
2. **一个逐题配对的受控语言对照实验**（§4.9）：在同一份官方平行语料上做 *state* 语言 × *criteria* 语言 2×2 交叉，四个效应**及其交互项（DiD）**均以配对 bootstrap 给出 95% CI 与显著性判定。这是本文强度最高的结果，**闭合了「语言效应会不会只是 criteria 措辞所致」这一最致命质疑**；
3. **一套人机混合的独立复核协作规程**（§3.6），并给出其有效性实证（0 项「不成立」／4 处自我更正／12 项结论撤回）；
4. **三条评测护栏 +「零信息实验」三形态 + 提前否证协议 D1–D4 + 阳性对照四件套 PC-1～PC-4**（§3.1～§3.3、§5.4）——**全部由本文自身的错误固化而来**；
5. **一份完整的负结果与错误台账**（附录 C），含 6 项至今未闭合的问题。

### 1.4 边界声明

本文测的是**第三方开源复刻实现**，非官方模型（官方未开源、无自托管）；结论**仅限现成 checkpoint、固定阈值、CPU-only、单模型**这一配置空间，不得外推为该类别的一般规律。详见 §7。

---

## 2 相关工作

> **本节由来（一次已登记的方法学教训）**：本文初稿定稿时**未做外部查新**，在外部提示下才补做同类工作普查，方知**外部同类工作远超本文**。本文据此**重新定位贡献**：本文不是「发现者」，而是「在一个新域（含中文）上的复现者 + 若干可复用测评护栏的提出者」。该教训已固化为一则通用规程：**研究成果交付前必做外部查新（≥3 个检索面），且检索须覆盖衍生名**。以下外部结论除注明者外**均标「外部·未复现」**——本文未对其做独立复现。

### 2.1 独立基准评测（与本文做法最接近，且更系统）

| 来源 | 日期 | 关键做法 | 关键结论（**均属「外部·未复现」**） |
|---|---|---|---|
| `decision-model-benchmark`（DMB） | 2026-09-23 | **3 类对手**（官方模型 ＋ 8 个受限 LLM ＋ 3 个确定性基线）、5 套件、60 个 cell、公开 $28.34 实测花费、公开全部原始日志、无厂商赞助 | ①**无人在质量上胜出**（banking 77-way：gpt-oss-120b 81.3%／glm-5.3 80.4%／**jev 76.3% 居中游**）；②**255 候选是硬边界**（254/255 达 100%，**256/384/512 直接 `400 Too many choices`**）；③**只有该模型「虚张声势」**：forced-uncertainty 题上 LLM 97.3–100% 承认不确定，**jev 仅 49.7%**，且**校准误差最差 ECE 0.246**（LLM 0.039–0.122）；④**位置偏置可测**：重排选项后选择改变 **13%** |
| `decision-index` | 2026-09-23 | 本地或云端作业复现基准套件 | — |
| `jev-baselines-eval` | 2026-09-23 | **预注册（pre-registered）＋ kill/go 判据 ＋ paired 95% CI**；对手＝nano LLM／前沿 LLM／**监督编码器**（Banking77 ＋ CLINC150） | 两个实验**均 AMBIGUOUS**；该模型是强零样本分类器（CLINC150 比 gpt-5.4-nano **+7.5 pp**，CI [+3.0, +12.5]）；**但延迟只快 2.2×**（0.42 s vs 0.92 s）；**有标注数据时 9 ms 监督编码器胜出**（笔记本、零调用成本） |
| `BioJev` | 2026-09-23 | 生物医学决策模型基准 ＋ **跨数据集泛化实验** ＋ 校准 | —（仅方法参照） |
| `jev-eval`（多个独立作者，≥16 个变体） | 2026-09-17 ～ 09-23 | 各自在自己数据上评测准确率／校准／阈值；其中一个中文作者**含「可用阈值」一环** | 评测该类模型已是**社区常规动作** |
| `jev-aita` | 2026-09-2x | 770 条 Reddit 语料；**weighted Brier score ＋ macro recall ＋ resampling CI**；**并对比不同 prompt 格式** | 该作者自我勘误「post-hoc 调整非 like-for-like」 |

### 2.2 同族模型的域外与跨语言测量（**对本文结论影响最大**）

| 来源 | 内容（**外部·未复现**） | 对本文的作用 |
|---|---|---|
| SOTAAZ《Jev in 10 Minutes》（三轮测量，A100，公开集） | 同族另一实现（421 M ModernBERT 编码器，**自称同用 RLCD 训练**）：**TREC（6 选项，不在其训练数据）86.6%**；**BANKING77（77 意图）37.0%**，归因＝**所有候选共享 token 预算、77 个标签互相挤压**；**且有解**：512-token 预算 → 46.1%；embedding 预筛至 20 候选 → **59.1%** | **直接反证「域外必坍缩」**，并暴露本文未控的一个变量：**候选数** |
| `dev.to` 一篇实现综述 | 指出**所有开源实现都复制「接口」，无人复现原始训练方法 RLCD**；`mini-jev` 自陈其数字是「带置信差距的排序，非校准概率」；von 自带 calibration file 与在自己数据上重拟合的脚本 | 促成本文核查 von 的校准路径（§4.2） |
| `fawadhs.dev`《How Jev Works, and How to Test a Decision Model》 | **「0.5 是约定，不是模型属性」**：Claude Haiku 4.5 在 0.5 阈值下 76.1%，**拟合阈值后 97.5%**；该类模型**非开箱校准**（7 次拟合温度落在 0.47–0.83，**欠自信**）；**TF-IDF ＋ 逻辑回归在最难任务上只差 0.5 pp，且零调用成本** | 使本文意识到「固定 0.5 阈值」是方法学缺陷（§7 局限 3） |
| `MindStudio`（行为层实测） | 该模型在 Minecraft 中「**quickly fell into repetitive loops, predicting the same actions over and over**」（连建 6 张工作台、住进自挖的洞）；归因＝**规划视野短**，非域外 | 提供「重复/坍缩」最接近的**行为层**外部观察 |
| Browser Use 创始人（长程浏览器测试） | **20 题只对 1 道**（对比推理型模型 17/20） | 同上 |
| 小马智行一位架构师（自动驾驶思想实验） | 改指令后仍选急刹（94%／77%／80%）⇒ **模型有自己的默认规则** | 支持「先验主导」解释 |

### 2.3 官方自陈的已知缺陷（**其自报·未复现**）

数数/算术不行｜**日期比较不可靠（日期被当文本读）**｜双重否定与间接引用掉准确率｜**臃肿 state 掉准确率（state 越大、无关内容越多，准确率越低）**｜**state 会被操纵性文本带偏（提示词注入有效）**｜`Score` 不是测量仪（1.4 分 ≠ 「40% 的愤怒」）｜**中文等非英语语言准确率较低（主要训练语言是英语）**

**⇒「非英语准确率较低」是官方承认的**，为本文 §4.6／§4.9 的语言效应提供了**同源解释**（但本文的语言效应另有独立配对检验支持，并非单纯引用官方口径）。

### 2.4 校准与阈值方法学（学术脉络）

现代神经网络普遍**校准不良且过度自信**，**温度标定（temperature scaling）**是最简且最有效的后处理校准方法，且**不改变 argmax**（Guo et al., 2017）。这为本文 §4.2 中「校准已加载但不改变答案」的实测提供了理论依据，也是本文 §7 局限 3（阈值未拟合）与 §5.2（置信度不携带信息）的直接方法学背景。

### 2.5 分布外静默失败的学术脉络（**本文初稿完全遗漏**）

| 来源 | 关联（**外部·未复现**） |
|---|---|
| 《Explanation audits reveal silent failures of machine learning models under distribution shift》（Springer，2026-08-24） | 给出「**静默失败**」的正式定义：**错误率显著上升而置信度告警信号未相应上升**；并量化了置信度类监控信号的排序能力（AUROC 0.8664） |
| MIT／NeurIPS 2025 一项研究（推翻「Accuracy-on-the-Line」假设） | 高平均准确率掩盖子群失败；「最佳模型」在新数据上可能变成最差 |
| 《Is Capability a Liability?》（arXiv:2605.22672，2026-05） | **逆缩放**：能力更强的模型在 regime change 处预测更差；且**标准指标看不到，尾部指标能反转结论** |

> **出处说明**：以上三条系经**二手转述**得见，本文**未取得其原始出处与完整可查标识**（作者、会议卷号、DOI 未核实）⇒ 仅作脉络提示，**不作引用依据**。

**⇒ 本文自陈的「高置信不携带信息」（§5.2）并非孤立发现**，而是这一脉络在「小决策模型」上的**一次复现**。

### 2.6 第三方验证协议（方法论参照）

`Assay Protocol v0`（**外部·未复现**）：对 NanoJev 逐帧重放轨迹，**1,044 次碰撞零不一致 ⇒ 判「agree」**。其核心主张是：**「决策模型的验证单元是整个轨迹（初始状态 ＋ 冻结规则 ＋ 声称终态，重放）」，而非单条回答**。该件同时自陈：**「第一次探针全红，先怀疑探针——是我们的 Windows CRLF 转换改了字节」**——与本文 §7 所记的一类环境型假阴性**同型**。

### 2.7 开源实现与生态规模

- **开源实现族**：`NanoJev`（596 M，基于 Qwen3-0.6B）、`von`（395 M，ModernBERT-Large）、`simple-jev`（把任何开放模型变成分类器）、`AnyJev`、`SemIf-OpenJev`、`jev-cookbook`（含 CLINC150 实测与 eval harness）、`Kev` 家族（0.8 B/4 B/9 B）
- **生态清单**：9 份 `awesome` 型清单（其中一份自称收录 834 个项目）
- **社区热度（本文以 Hacker News 官方 API 一手实测）**：`Kev` **459 分/200 评**；`JevBench` **140 分/36 评**；`CUA-S1` 93 分；`OpenJev` 600+ 分
- **同期发布的社区版本 `laya-mlx`**：**仅 Apple Silicon**（MLX）⇒ 本文的两台 Windows 机器**直接不可用（未实测）**；其官方自述 base checkpoint **低于多数类基线**（0.362/0.342 vs 0.461，**其自报·未复现**）

> **一处本文此前结论的更正**：本文早期记录曾判「该话题在 Hacker News 未被接住（分数 1–5、评论近零）」——**该结论错误**，错因是**检索词过窄**（只检索主名，未覆盖 `Kev`／`JevBench` 等衍生名）。

### 2.8 合规声明（**照原样保留，不予引用、不予展开**）

普查过程中发现一份**以我国主权议题为题**立场的测试件。本研究按合规要求**不予引用、不予展开、不予采用**；此处仅登记「存在该件」，以免后续重复检索。

---

## 3 方法

### 3.1 三条评测护栏

| # | 护栏 | 防的是什么 |
|---|---|---|
| ① | **正负样本平衡** | 给「退化模型」留后门——退化模型会以 100% 命中率通过不平衡题集 |
| ② | **附基线对照**（恒答 True／False／多数类） | 把「基线水平的正确率」误读为「有判断力」 |
| ③ | **报预测分布**（不只报准确率） | 准确率 50% 既可来自「完全不会」，也可来自「会一半」 |

> 三条护栏**不是先验设计**，而是由本文自身两次失败固化而来（附录 C·C.1／C.2）。

### 3.2 判据设计：提前否证协议 D1–D4

**动机**：深度评测的代价远高于粗筛。**闸门段规模**：10 类 × 3 in-scope ＋ 30 out-of-scope ＝ **60 次前向**，若命中否证判据即中止，省下后续全部开销。

| 判据 | 观测 | 初版 → 修订后 |
|---|---|---|
| **D1** | 预测分布的唯一值占比／熵异常低 | 初版用「唯一值 ≤ 1」，**已知漏网**（实测唯一值 = 2 即未命中）⇒ 改为**分布统计量判据** |
| **D2** | AUC 与 0.5 **不可区分**（95% CI 含 0.5） | 语义明确为「**未能证明具备判别力**」，而非「证明有反能力」 |
| **D3** | in-scope 检出率 vs 随机基线 | 初版**表述方向错误**：判断「无能力」应作**等价检验**；显著性阈值会把「样本量不足」误判为「对象无能」⇒ 改为明确标注「**仅作提示，不作证明**」 |
| **D4** | *p* > 0.9 占比 > 95% 而准确率 ≈ 基线 | 高置信欺骗 |

> **阈值依据**：均为**经验值**，无功效分析（power analysis）⇒ **D1–D4 的正确用法是「筛除」，不是「证明」**。

### 3.3 阳性对照四件套 PC-1～PC-4

**动机（本文自认的「最致命缺陷」）**：若无阳性对照，则**「探针/链路问题」与「模型在目标域退化」不可区分**。

| 编号 | 设计 | 作用 |
|---|---|---|
| **PC-1** | 原生域对照（用该模型训练分布内的公开集跑同一链路、同一探针） | 区分「链路/探针问题」与「域不匹配」 |
| **PC-2** | **输入敏感性**：三条件（原陈述／语义取反／无关句）各 12 条 | 若三者近乎同值 ⇒ **输入未被使用**（链路级假阴性） |
| **PC-3** | *criteria* 标签互换 | ⚠️ 见 §5.4：**该设计事后被证明为零信息实验** |
| **PC-4** | 英文 held-out 200 题 | 区分「中文题面问题」与「模型问题」 |

### 3.4 数据集、映射与**标签语义核实规程**

| 数据集 | 用途 | n | 映射规则 |
|---|---|---|---|
| 自造中文短规则命题 | 自有域 | 12 | 正负各半 |
| CLINC150 | 域外拒识 | 60（闸门段） | `Noul` 二分类 |
| OCNLI（可得版本） | 备选（**未采用**） | 1,847 | 该版本**中立已与矛盾合并** ⇒ 无法执行「丢弃中立」规则 |
| **CLUE → CMNLI validation** | **中文公开集主集** | 12,241（取 200／300） | **`0 = neutral｜1 = entailment｜2 = contradiction`** |
| **MultiNLI → validation_matched** | **英文原版对照** | 9,815（取 200／300） | **`0 = entailment｜1 = neutral｜2 = contradiction`** |
| **BoolQ → validation** | **英文原生域对照** | 3,270（取 200） | 原生 yes/no |
| **XNLI → zh/en validation** | **逐题配对的跨语言材料** | 各 2,490（取 200/格；**行数已核实**：HF 卡片 `splits.validation.num_examples`） | **`0 = entailment｜1 = neutral｜2 = contradiction`** |

**★ 标签语义核实规程（本次拦下一个会反转结论的错误）**：

1. **不得凭样例语义自行判定标签编码**；
2. **必须与该数据集的权威文档示例逐字比对**——本次做法：CLUE 官方文档给出示例 `{"sentence1":"新的权利已经足够好了","sentence2":"每个人都很喜欢最新的福利","label":"neutral"}`；本文最初把该样例判为「矛盾」，**若照此执行则「蕴含 = yes／矛盾 = no」整体错位、结论方向反转**；
3. **鼓励跨数据集交叉验证**：同一对句子在 MultiNLI 标 `1`、在 CMNLI 标 `0`，语义一致 ⇒ **互证两套编码**。

**★ 两套映射敏感性（零额外前向）**：由于标签映射是**标签侧操作、不改变模型前向**，同一批 `noul` 分数可用两套规则并行评估：

- 规则 **A「丢弃中立」** ＝ entailment (True) vs contradiction (False)；
- 规则 **B「合并中立」** ＝ entailment (True) vs (contradiction ＋ neutral) (False)。

两套都报 ⇒ 避免「选一个规则」引入的任意性。

### 3.5 统计口径

- **AUC** 与其 95% CI：bootstrap 重采样 2,000 次，`seed = 17`；
- **组差**＝ *P*(yes)（正样本均值）− *P*(yes)（负样本均值），并报其 95% CI；
- **配对 ΔAUC**（§4.9）：以**同批 index 逐题配对**重抽，四格同步重算，得到每个效应的 ΔAUC 及其 95% CI；
- **交互项（DiD）**（§4.9）：对「`criteria` 效应之差」＝ (L3−L4) − (L2−L1) 另做同款配对 bootstrap，**以该差自身的 CI 判定交互**，不以「一项显著、一项不显著」代替（**显著性之差 ≠ 差异的显著性**）；
- **多重比较**：§4.9 的 5 个检验**未做多重比较校正**；以 Bonferroni α = 0.05/5 = 0.01 作保守判读（三个显著项下界 0.2427／0.1074／0.1104、交互项下界 −0.2086 距 0 均远）⇒ **结论不因校正而翻转**；
- **机器可判的坍缩判据**（预注册，先于实验确定）：

```
collapse     = (AUC 95%CI 上界 ≤ 0.60) && (|组差| < 0.05 且其 CI 含 0) && (acc 95%CI 含多数类基线)
not_collapse = (AUC 95%CI 下界 > 0.60)
其余          = 灰区 ⇒ 写「不可判定」，不得强行归因
```

- **措辞纪律**：CI 含 0.5 时**只能**写「未能证明具备判别力／与随机不可区分」，**不得**写「低于随机／方向反向」。

### 3.6 独立复核协作规程（**程序化**）

本文全流程由**两个异构 AI 系统**分工：**执行方**（本文第一作者）负责实测与撰写；**独立复核方**为一个**由不同厂商提供的通用大语言模型系统**，**只接收执行方提供的文本与数据**，独立给出判断，**不接触执行方的原始文件**；**方向裁定方**为一名人类决策者，作最终方向裁定与授权。

**规程（由多轮往复交互提炼）**：

1. **执行方出任务书**：必含「待评件全文或路径 ＋ 背景事实（**不给结论**）＋ 明确要求『独立评估、不要附和作者』＋ 具体问题清单（≥5 问）＋ 允许动作（默认只读）」；
2. **复核方出意见**：结论先行 ＋ 逐条举证 ＋ 区分「已证／推断／不可判」；
3. **执行方：禁照单全收**——每条逐条判断「成立／部分成立／不成立」并写明依据；接受的当场改，不接受的写明理由；**架构级问题报方向裁定方**；
4. **经验回收**：通用教训入经验台账，判据/门的设计缺陷回写对应工具。

**规程的有效性实证**：往复交互中，**5 轮否证或修正了执行方的判断**，其中包括一处**统计上的硬错**与一处**过度解读**；执行方据此**自我更正 4 处判断、撤回 12 项结论**（附录 C）。同时，**复核方自身的意见也被否证过 1 次**（§4.4 的符号方向问题，实测与源码注释一致，复核方的推断不成立）——**规程是双向的，不是单向接受**。

**四条纪律（本次固化）**：

1. **禁照单全收**：每条须回实体核并判「成立／部分成立／不成立」；
2. **禁因「外部」就放弃判断**：复核方不了解全部历史，其「推断」项须回实体核；
3. **架构级报裁定方**：方向性裁定不由复核方或执行方代作；
4. **复核意见本身不是证据**：复核结论只有落到实测或可查材料上方可入文。

---

## 4 实验与结果

> **累计 2,640 次前向**（逐项核算见 §4.11）｜全部 **CPU-only**（AMD Ryzen 7 8845HS 8C16T，无 GPU；两台 Windows 11 机器）
> 模型：`NanoJev`（596.3 M，基于 Qwen3-0.6B）与 `von`（395 M，ModernBERT-Large，权重版本 1.2）

### 4.1 实验一：NanoJev（596.3 M）——预测标签单侧

| 项 | 值 |
|---|---|
| 加载 / 推理 | 6.1 s / 首批 6 题 7,310 ms（≈ 1.2 s/题） |
| 官方入口可用性 | ❌ 其 `predict_toy_decisions.py` **硬断言要求 CUDA**，无 CPU 回退 ⇒ **本文自写探针** |
| 12 题（正负各半） | 预测 True **12/12**；准确率 6/12 = **50.0%** ＝ 恒答 True 基线 |
| `p_true` 分布 | 全部落在 **0.664–0.896**，**无一题低于 0.5** ⇒ **高置信但不携带信息** |

### 4.2 实验二：von（395 M）——自有域

| 配置 | 准确率 | 预测 True | `noul` 区间（均值） |
|---|---|---|---|
| 中文 12 题 | 6/12 = **50.0%** | 0/12 | 0.2667–0.4741（0.3175） |
| 英文 12 题 | 7/12 = 58.3% | 1/12 | 0.1447–0.7337（0.2959） |
| 中文 12 题 · **不传 `criteria`**（官方零样本先验路径） | **6/12 = 50.0%** | **0/12** | 0.2437–0.3225（0.2727） |

**宣称兑现情况**：原生 CPU 加载 ✅｜其自报 ~18 ms 延迟 ❌（实测 **0.27–0.45 s/题**）｜order-invariance（其 1.2 版自称架构层修复）**不可判**（见 §5.4）。

**校准路径核查（执行方主动做，用于排除「配置偏差」）**：

- 运行时输出含 `(input-conditioned calibration map active)` ⇒ **校准已自动加载**；
- SDK 源码注释：*"Temperature is monotonic, so this **never moves the argmax**"* ⇒ **校准不改变答案**（与 Guo et al., 2017 的理论一致）；
- SDK 对「不传 `criteria`」会走官方 `noul_zero_shot_prior` 去偏路径 ⇒ 本文实跑对照：**结果同向**（仍 6/12、预测 True 0/12，且概率被进一步推向 "no"：均值 0.3175 → 0.2727）⇒ **「接口用反／配置偏差」被排除**。

### 4.3 实验三：CLINC150 域外拒识（闸门段 · 60 题）

| 指标 | 实测 | 对照 |
|---|---|---|
| 总准确率 | **29/60 = 48.3%** | 随机 50.0%｜**恒答「域外」基线 30/60 = 50.0%** |
| in-scope 判为域内 | **1/30 = 3.3%** | 随机基线 50% ⇒ **显著偏向「域外」** |
| **AUC** | **0.4700** | SE ≈ 0.075 ⇒ **95% CI ≈ [0.32, 0.62]，含 0.5** |

⚠️ **本项的正确表述**：AUC **与 0.5 不可区分** ⇒ **未能证明具备拒识判别力**；**不可**写作「低于随机／方向反向」（本文初版曾如此写，与自己的 D2 判据自相矛盾，**已更正**）。

### 4.4 实验四：PC-2 输入敏感性 ＋ PC-3 标签互换（12 题）

**PC-2**：均值 (a) 原陈述 **0.3175** ／ (b) 语义取反 **0.2681** ／ (c) 无关句 **0.2796**

- **取反 ≈ 无关句**（差 0.0116）；**6/12 题对取反完全无反应**（|Δ| < 0.02）⇒ **模型对「内容是什么」几乎不响应**；
- 但「原陈述」比另两者高约 0.04–0.05 ⇒ **对「有没有内容」仍有微弱响应**（故不构成「输入完全未读入」）。

**PC-3**：A = 0.3175／B = 0.6825；**`A + B = 1.0000`（12/12 精确）**

- ⚠️ **该结果是二分类 softmax 的结构性恒等式**（*criteria* 互换**必然**互补，**与模型行为无关**）⇒ **零信息**：**既不能排除位置作答，也不能排除接口用反**（本文原据此判「排除接口用反」，**已撤回**，见 §5.4）。
- 由两组数据可读出的等效信息只有一句：**无论标签排第几，模型都给「该说法错误」约 0.68、给「该说法正确」约 0.32**——即一个**稳定的负向偏置**，与标签位置无关。

**附：一处对复核方意见的否证**。复核方曾据源码注释与随包先验参数（`a = −0.5, b = 0.3`）推断「文本表述与实际方向矛盾」。本文以 12 条同题实测：**不传 criteria 均值 0.2727 ＜ 传 criteria 0.3175，Δ = −0.0448**，即去偏把 yes 侧推**低**，与源码注释 *"pulls predictions toward no"* **方向一致** ⇒ **复核方该条推断不成立**（其推断基于 bias > 0.6 的假设，本批题未达该量级）。

### 4.5 实验五：中文公开集 CMNLI（n = 200／300）

数据：`CLUE → cmnli/validation`，全量 12,241 条（neutral 3,926／entailment 4,277／contradiction 4,038）；分层抽样 `seed = 17`。

| 规则 | n | 准确率 | 基线 | **AUC [95% CI]** | **组差 [95% CI]** | 判定 |
|---|---|---|---|---|---|---|
| A·丢弃中立 | 200 | 64.0% | 50.0% | **0.6680** [0.5952, 0.7405] | **+0.0712** [+0.0391, +0.1001] | **灰区** |
| B·合并中立 | 300 | 64.7% | **66.7%** | 0.6218 [0.5524, 0.6945] | +0.0518 [+0.0218, +0.0802] | 灰区 |

⚠️ **规则 B「准确率低于基线」的正确解读**：64.7% ＜ 66.7% **不是模型更差**（AUC 0.6218 的 CI 仍不含 0.5），而是**基线被中立并入后抬高到 66.7%**——这是「合并异质标签」的直接代价，**与理论预期一致**（§5.3）。
**并列原因（不可省略）**：全程使用**固定 0.5 阈值**，而该阈值**未随合并后的类别分布重新校准** ⇒ 准确率与基线的比较在此配置下不构成判别力证据（与 §7 局限 3 同源）。

### 4.6 实验六：PC-1 英文原生域对照 —— BoolQ（n = 200）

数据：`google/boolq → validation`（3,270 条）。

| 指标 | 值 |
|---|---|
| 准确率 | **164/200 = 82.0%**，95% CI [76.1%, 86.7%] |
| 多数类基线 | 50.0% |
| **AUC** | **0.8922** |
| **组差** | **+0.1814** |
| 混淆矩阵 | TP = 85／FP = 21／FN = 15／TN = 79 |
| 规模 | 260 次前向／71.2 s |

**⇒ 结论：链路正常**（同一探针、同一链路在原生域上给出强判别力）⇒ **「探针错误／链路失效」被排除**，剩余差异指向**域与语言场景**。
⚠️ **污染嫌疑未排除**：BoolQ 公开于 2019，该 checkpoint 的训练语料**可能包含该集** ⇒ 82.0% **不得作为泛化能力证据**（但**不推翻「链路通」**这一结论）。

### 4.7 实验七：英文原版对照 MultiNLI（n = 200／300）★ 关键对照

| 规则 | n | 准确率 | **AUC [95% CI]** | **组差** | 判定 |
|---|---|---|---|---|---|
| A·丢弃中立 | 200 | **81.0%** | **0.9532** [0.9194, 0.9796] | **+0.2537** | **not_collapse（有判别力）** |
| B·合并中立 | 300 | 56.3% | 0.5779 [0.5157, 0.6405] | +0.0660 | 灰区 |

**⚠️ 混淆声明（依复核意见更正）**：本实验**不是「只换 state」**——实测两脚本的 `criteria` 语言也不同（CMNLI 用中文 criteria／MultiNLI 用英文 criteria）⇒ **变量是两个**（`state` 语言 ＋ `criteria` 语言）。应表述为「**整个语言场景同步切换**」，该混淆**在本实验中未分离**（分离见 §4.8／§4.9）。

### 4.8 实验八：2×2 交叉（`state` 语言 × `criteria` 语言）

**设计**：因素 A ＝ *state* 语言（中文 CMNLI ／ 英文 MultiNLI）；因素 B ＝ *criteria* ＋ *instructions* 语言（中：`该说法正确/错误` ＋ `请判断该说法是否成立。`｜英：`Yes/No` ＋ `Judge whether the statement holds true.`）。每格 n = 200（entailment 100 ＋ contradiction 100，`seed = 17`，同一批条目在四格间固定）；**800 次前向 / 188.5 s**。

| 格 | 配置 | 准确率 | **AUC [95% CI]** | **组差 [95% CI]** |
|---|---|---|---|---|
| **G1** | 中 state × **中** criteria | 64.0% | **0.6714** [0.5963, 0.7478] | +0.0742 [+0.0407, +0.1067] |
| **G2** | 中 state × **英** criteria | 74.0% | **0.8105** [0.7513, 0.8672] | +0.1276 [+0.0992, +0.1560] |
| **G3** | 英 state × **英** criteria | 95.0% | **0.9785** [0.9545, 0.9945] | +0.3910 [+0.3624, +0.4170] |
| **G4** | 英 state × **中** criteria | 92.0% | **0.9668** [0.9379, 0.9890] | +0.3279 [+0.2976, +0.3552] |

**边际差（以 AUC 置信区间中点计）**：`state` 语言 0.7407 → 0.9690，**−0.2283**；`criteria` 语言 0.8177 → 0.8919，**−0.0741**。

⚠️ **对「边际分解可加」的明确否认**：上述两分量相加约等于原对比的总差（0.2283 ＋ 0.0741 ≈ 0.302），**但该和恰好等于对角差 G3 − G1 = 0.307，由边际分解的构造本身保证 ⇒ 属近似恒等式，不构成可加性证据**；四格的交互项（DiD ＝ 0.0117 − 0.1391 ≈ **−0.127**）与其方向相反，且该交互量级**大于 criteria 主效应本身**（0.0741），说明**存在强交互**（英文侧接近天花板，压缩了 criteria 效应）。**正式结论以 §4.9 的配对 ΔAUC 检验为准。**

**⚠️ 局限**：中英两侧使用的是**不同数据集**（CMNLI vs MultiNLI，虽同属 NLI 句对且 CMNLI 部分译自 MultiNLI，但**非逐字配对**）⇒ 交叉解读**仅限效应量级比较，不等于逐题受控**——逐题受控由 §4.9 解决。

### 4.9 实验九：XNLI 逐题配对 2×2 ＋ 配对 ΔAUC（★ 本文最强结果）

**为什么需要本实验**：§4.8 的 `state` 语言效应与「数据集来源」**捆绑**（CMNLI 与 MultiNLI **非同题**）。改用 **XNLI** 材料——`facebook/xnli` 的 `zh/validation` 与 `en/validation` **官方平行、同 index 同题**（两者均 2,490 行、label 分布相同），**逐题配对**即成立。

| 格 | 配置 | **AUC** |
|---|---|---|
| **G5** | 中 state × **中** criteria | **0.6430** |
| **G6** | 中 state × **英** criteria | **0.8082** |
| **G7** | 英 state × **英** criteria | **0.9734** |
| **G8** | 英 state × **中** criteria | **0.9569** |

（n = 200/格：entailment 100 ＋ contradiction 100，`seed = 17`；**四格使用同一批 index**；**800 次前向 / 180.6 s**）

**★ 配对 ΔAUC 检验**（配对 bootstrap 2,000 次，`seed = 17`）：

| 效应 | **ΔAUC** | **95% CI** | 判定 |
|---|---|---|---|
| **`state` 语言效应 @ 中文 criteria** | **+0.3150** | **[+0.2427, +0.3884]** | **显著** ✅ |
| **`state` 语言效应 @ 英文 criteria** | **+0.1660** | **[+0.1074, +0.2257]** | **显著** ✅ |
| **`criteria` 语言效应 @ 中文 state** | **+0.1654** | **[+0.1104, +0.2248]** | **显著** ✅ |
| `criteria` 语言效应 @ 英文 state | +0.0164 | [−0.0046, +0.0392] | **不显著**（CI 含 0） |
| **★ 交互项 DiD（上两行之差）** | **−0.1487** | **[−0.2086, −0.0916]** | **显著** ✅ |

同批数据下的**配对点估计**：`state` 效应固定中文 criteria **+0.3139**／固定英文 criteria **+0.1652**；`criteria` 效应固定中文 state **+0.1653**／固定英文 state **+0.0165**。

**三条结论**：

| # | 结论 | 强度 |
|---|---|---|
| 1 | **`state` 语言效应成立，且在两种 criteria 语言下均显著** ⇒ **criteria 混淆不足以解释语言效应** | **一手实测·配对正式检验** |
| 2 | **`criteria` 语言效应只在中文 state 下显著**（+0.1654），在英文 state 下不显著（+0.0164）；**交互项 DiD ＝ −0.1487，95% CI [−0.2086, −0.0916]（不含 0）** ⇒ **交互经正式检验成立，非由「一项显著、一项不显著」推断** | **一手实测·配对正式检验** |
| 3 | **与前次非配对 2×2 逐项一致**（+0.2954／+0.1680／+0.1391／+0.0117）⇒ **效应估计稳健** | 一致性检验 |

> **本节新增于 v2**：交互项 DiD 系**分析层重算**（零新增前向，用本节已落盘的逐题分数）；重算实现先以「复现四个简单效应的 95% CI」自校验，**四项逐位一致** ⇒ 计算路径可信。

**⇒ 本文此前「最致命质疑」（变更 `state` 语言的对照同时变更了 `criteria` 语言，因而无法归因）至此完全闭合。**

**局限**：XNLI 的 validation **亦是翻译集**（源自 MultiNLI）⇒ 「语言」与「译本风格」仍不可完全分离（比 CMNLI vs MultiNLI 干净，但**非原生中文**）；`criteria` 与 `instructions` **捆绑切换**，未分离两者；英文侧 AUC 已近天花板（0.957–0.973）⇒ 交互的**绝对量级**受此压缩。

### 4.10 实验十：自有 12 题仅替换 `criteria` 语言（**负结果**，12 次前向）

| 配置 | 准确率 | 预测 True | `noul` 均值 |
|---|---|---|---|
| 中文 criteria（原） | 6/12 = **50.0%** | 0/12 | 0.3175 |
| **英文 criteria（新）** | 6/12 = **50.0%** | **0/12** | **0.2350** |

**⇒ 自有域毫无改善**（**准确率与预测标签分布不变**：仍 6/12、仍预测 True 0/12；`noul` 均值 0.3175 → 0.2350，即**分数确有移动、但未改变判定**）。

**⇒ 由此得出一条重要边界**：「**中文 criteria 有害**」这一效应**只在 NLI 形态下成立**（XNLI 中 +0.1654），**在自造规则命题上不成立**（准确率与预测标签 0 变化）⇒ **两域对 criteria 语言的响应不同**（**推断**：n = 12 只能支持「自有域低分与 criteria 语言无关」，不足以断言机制层差异），自有域低分**与 criteria 语言无关**，**不可**用「换英文 criteria 可改善」来解释自有域表现。

**⇒ 该结果反而支持**「自有域低分源于**题面形态／自造出题**，而非语言配置」这一方向。

### 4.11 汇总与前向数核算

**四行总表**（可直接比较者为前三行与第四行分别评述）：

| 语言 | 数据集 | 形态 | n | 准确率 | **AUC [95% CI]** | 组差 | 判定 |
|---|---|---|---|---|---|---|---|
| **英文** | **MultiNLI** val_matched | NLI 句对 | 200 | **81.0%** | **0.9532** [0.9194, 0.9796] | +0.2537 | not_collapse |
| 英文 | BoolQ validation | 段落问答 | 200 | 82.0% | 0.8922 [—] | +0.1814 | not_collapse（**污染嫌疑未排除**） |
| **中文** | **CMNLI** validation | NLI 句对 | 200 | 64.0% | **0.6680** [0.5952, 0.7405] | +0.0712 | 灰区 |
| 中文 | 自造短规则命题 | 规则命题 | 12 | 50.0% | ≈ 0.5 | −0.0148 | —（n = 12 不可比） |

**⇒ 级差（描述性）**：**英文原生域（0.89–0.95）➜ 中文公开集（0.67）➜ 自造题（≈ 0.5）**。
**⚠️ 强度纪律**：跨形态（BoolQ vs CMNLI）不可直接比较；MultiNLI（＝ MNLI）与 CMNLI 之间的「明显高于」依据 **CI 不重叠**（保守判据），**该对比未做 ΔAUC 两样本正式检验** ⇒ **不得写作「显著优于」**；**跨语言的正式证据见 §4.9**（XNLI 配对检验下 `state` 效应显著）。

**前向数逐项核算**：

```
NanoJev 自有域            12
von 自有域（12 题 × 2 语言）  24
von 不传 criteria 对照        12
CLINC150 闸门段 A             60
PC-2 输入敏感性（12 × 3）     36
PC-3 标签互换（12 × 2）       24
PC-1 BoolQ（200 ＋ 标签侧 60）260
CMNLI（两套映射共用同一批前向）300
MultiNLI                      300
2×2 交叉（4 × 200）          800
12 题换英文 criteria          12
XNLI 配对 2×2（4 × 200）     800
─────────────────────────────────
合计                        2,640
```

> **更正**：本文早期版本记载为 1,092 次前向，**该数算不平**；上表为逐项重算后的正确总数。

### 4.12 抽样稳定性（一个副产物）

`ocnli_eval` 与 `2x2_cross` 两个脚本**同 seed 但随机数消费路径不同**（前者按 `neutral→entailment→contradiction→shuffle` 的顺序消费，后者按 `zh_ent→zh_con→en_ent→en_con→shuffle(zh)→shuffle(en)`），导致各抽 200 条的**交集仅 5 条**。

**⇒ 两者 AUC 差 0.0034 不是 bug，而是抽样差异**；且**几乎完全不同的抽样只使 AUC 变化 0.0034** ⇒ 该配置的点估计相当**稳定**（副产品结论：**n = 200 下抽样波动约 0.003–0.004**，可作为稳定性说明写入方法）。

---

## 5 讨论

### 5.1 归因

| 序 | 因素 | 依据 | 强度 |
|---|---|---|---|
| **①** | **语言（场景）** | **逐题配对的正式检验**（§4.9）：`state` 效应在两种 criteria 语言下均显著（+0.3150／+0.1660，CI 均不含 0）；`criteria` 效应在中文 state 下显著（+0.1654）；**交互项 DiD −0.1487（CI 不含 0）** | **一手实测·配对检验** |
| ② | **题面形态／自造出题** | CMNLI 0.6680 → 自造 ≈ 0.5（Δ ≈ 0.17），但 **n = 12 不足以判定显著**（其 AUC 的 95% CI ≈ ±0.28 ⇒ **包含 CMNLI 的 0.668**） | **不可判** |
| ③ | 域内容 | 未单独受控 | 待验证 |

> ⚠️ **「样本量」不列为因素**：**n 影响置信区间，不产生点估计的期望差** ⇒ 它只贡献**不确定性**，不是「效应来源」。**本文早期版本曾将其列为差异来源之一，已更正。**
> ⚠️ **「题面形态」的归因须加边界**：本实验只能证明「**中文单独不是坍缩的充分条件**」（CMNLI 未坍缩）；**不能**证明「与中文无关」——**语言 × 题面形态的交互项未被排除**。

### 5.2 「高置信输出不携带信息」——本文最实用的警告（**复现，非首创**）

NanoJev 的 `p_true` 全部落在 0.664–0.896，**无一题低于 0.5**；von 则全部低于 0.474。若只看「概率高」，会得出「模型很有信心、判断可用」的结论——**这是完全错误的**。

**该现象的外部强化（外部·未复现）**：DMB 独立实测官方模型在 forced-uncertainty 题上**仅 49.7% 承认不确定**（LLM 97.3–100%），**ECE 0.246（最差）**。
**⇒ 本文的贡献应降级为「在一个新域上的复现」**，并引用 DMB 与校准文献（Guo et al., 2017）。

**结论**：**未经校准的置信度不携带信息**。引用任何模型的置信度时，**必须同步给出校准证据**（ECE／Brier／可靠性图）。

### 5.3 「合并异质标签会衰减 AUC」（方法学注意点）

两例方向一致：MultiNLI 0.9532 → 0.5779；CMNLI 0.6680 → 0.6218。
机制清楚：把中性（自有分数分布）并入 "no" 类，使负类**类内异质化**，等同于引入标签噪声 ⇒ AUC 衰减。
**措辞纪律**：**n = 2 例不构成「系统性」** ⇒ 写「**与理论预期一致的两例支持**」，并附「准确率低于基线」的类分布解释（该解释还须并列声明阈值未随新类分布重校准，见 §4.5）。

### 5.4 ★ 「零信息实验」的三种形态（**本文最主要的方法学产出**）

本文在四个环节踩到、并逐一纠正：

| 形态 | 本文实例 | 纠正来源 |
|---|---|---|
| **① 退化必呈现型** | 当输出对输入无依赖时，**顺序扰动「翻转 0/12」必然成立** ⇒ 不能作为「order-invariance 修复生效」的证据 | 复核方 |
| **② 恒等式型** | *criteria* 互换下 **`A + B = 1.0000`** 是**二分类 softmax 的必然**，与模型行为无关；**与「模型完全忽略输入」完全相容**，**接口真用反也照样成立** | 复核方 |
| **③ 阈值型** | 用「唯一值 ≤ 1」**判常量**（而实测唯一值 = 2 ⇒ 漏网）；用 200 字阈值判 78 字短回答（恒假） | 执行方自查 ＋ 复核方 |

**可复用三问（下结论前逐条过）**：

1. 该关系是**算法/数学的必然**，还是**对象的行为**？
2. 若对象**完全失效**，该关系是否**照样成立**？
3. 是否存在**第二个解释**能同样产生该关系？

—— **任一命中 ⇒ 判为零信息，不得作为能力证据。**

### 5.5 本文的两类系统性错误（诚实记录）

| 类别 | 实例 | 固化护栏 |
|---|---|---|
| **证据强度未标定** | 以 n = 2 断言「系统性」；对单机 12 题使用「铁证」；把结构性恒等式当行为证据 | **强度词前置标定**（须先写明「该证据类型能支持的最高强度」）＋ **零信息实验三问** |
| **不查既有成果** | 初稿定稿**未做外部查新**；重复评测既有工具能力 | **研究成果交付前必做外部查新（≥3 个检索面）**；**检索须覆盖衍生名** |

### 5.6 诊断「对端无响应」的一条通用护栏（由工具侧改造抽出）

研究过程中连续三次出现「复核方无响应」，实测**实为执行方自身的消息投递工具把成功当失败**（成功判据阈值量级不匹配、时间单位混用致兜底恒不触发、会话目录硬编码）。修复后 10 秒内即可取回正文。

**⇒ 通用护栏**：**诊断「对端无响应」时，先证我方接收端无缺陷（阈值／管道／路径），再怀疑对端。**

---

## 6 结论与建议

### 6.1 结论（分级）

| # | 结论 | 强度 |
|---|---|---|
| 1 | **`state` 语言效应成立，且在两种 criteria 语言下均显著**（逐题配对 ΔAUC **+0.3150** 与 **+0.1660**，95% CI 均不含 0）⇒ criteria 混淆不足以解释语言效应 | **一手实测·配对正式检验** |
| 2 | **`criteria` 语言效应只在中文 state 下显著**（**+0.1654**），在英文 state 下**不显著**（+0.0164，CI 含 0）；**交互项 DiD ＝ −0.1487，95% CI [−0.2086, −0.0916]（不含 0）** ⇒ **强交互（交互项经配对正式检验，非推断）** | **一手实测·配对正式检验** |
| 3 | **中文公开集上存在弱但显著的判别力**（AUC 0.6680，组差 CI 不含 0）；按预设机器判据落**灰区** | 一手实测 |
| 4 | **英文原生域明显高于中文**（MultiNLI 0.9532 vs CMNLI 0.6680，**依 CI 不重叠的保守判据**；BoolQ 0.8922 与前述**形态不同、不可直接比较**）——**该对比未做 ΔAUC 两样本正式检验**，故为**描述性**表述；**跨语言的正式证据由 §4.9 提供**（XNLI 配对检验下 `state` 效应显著：+0.3150／+0.1660） | 一手实测（描述性；正式证据见 §4.9） |
| 5 | **高置信输出不携带信息**（本域复现；外部 DMB 与校准文献已强化） | 一手实测 |
| 6 | **自有域 12 题未检出判别力**（AUC ≈ 0.5、组差 **−0.0148**）——**n = 12 检验力不足，仅作探索性观察**，**不可作坍缩确证** | 一手实测（强度受限） |
| 7 | von 的自报 ~18 ms 延迟**未兑现**（实测 0.27–0.45 s/题） | 一手实测 |
| — | ~~「两个模型各自独立退化 ⇒ 系统性」~~ | **不予断言**（n = 2，且语言效应主导） |
| — | ~~「输出坍缩」~~ | **措辞撤回**：应写「**未检出判别力（n = 12，检验力不足）**」 |

### 6.2 结论与建议（**由绝对归档改为条件归档**）

> 「该类模型在**自造中文短规则命题 ＋ 固定 0.5 阈值**这一用法组合下**未检出判别力**，且与中文公开集上判别力偏弱（CMNLI 0.668，同形态英文 0.953）一致，故**建议在归档判定中限于该用法组合**；**模型整体判别力不予归档**（MultiNLI 0.9532／BoolQ 0.8922 为反证）。」

### 6.3 若未来重启该路线，须先满足

1. 存在 **CPU 原生、有公开基准、有独立复现** 的实现；
2. 数据构造**全程只用公开内容**；
3. **先跑阳性对照 PC-1／PC-2**（证明测的是模型而非探针），再跑 D1–D4；
4. 在**模型原生形态**上测，并**统一语言场景**（避免本文 §4.7 的 criteria 语言混淆）；
5. **报告三件套**：**AUC ＋ 组差 ＋ 多数类基线**（而非单报准确率）；
6. **拟合阈值并同时报 AUC 与校准指标（ECE）**——0.5 是约定，不是模型属性。

---

## 7 局限（★ 禁止越界解读）

**A 类 · 可能推翻结论的**

1. **12 题样本的检验力不足**：其 AUC 的 95% CI ≈ **±0.28** ⇒ 连 AUC = 0.7 都不能拒绝；**该行的「未检出」不等于「不存在」**；
2. **未做阈值拟合**：全程使用固定 0.5 阈值。外部证据表明 **0.5 是约定**，拟合阈值可显著改变结论 ⇒ **本文「预测单侧」的结论可能是阈值不当的产物（未排除）**；
3. **CMNLI vs MultiNLI 的语言混淆**：`state` 语言与 `criteria` 语言**同步改变**（§4.7 已声明；§4.8／§4.9 已用交叉设计与配对检验分离，但**该对比本身仍未逐题受控**）；
4. **BoolQ 的污染嫌疑未排除**：82.0% 不得作为「泛化能力」证据（但**不推翻「链路通」**）；
5. **两模型均为第三方开源复刻实现**，非官方模型（官方未开源、无自托管）；
6. **权重 revision 未锁定**，且 12 题的**题面与 prompt 模板原文未留档** ⇒ **第三方无法完整复现**（本文**显式登记为复现缺陷**）。

**B 类 · 限制外推范围的**

7. **语言/形态/样本量三者未完全解耦**；8. **所测中文数据集多为翻译集**（CMNLI 译自 MultiNLI／XNLI，XNLI validation 亦然）⇒ 非原生中文；9. **未测 `Choice`／`Score` 原语的完整能力**；10. **未做微调验证**（结论限定于现成 checkpoint）；11. **全部 CPU-only**，未测 GPU 下的延迟/校准差异；12. **`criteria` 与 `instructions` 捆绑切换**，未分离二者；13. **CLUE 数据集未标注许可条款** ⇒ 复用时应另行确认（附录 A）。

---

## 8 复现指引

**环境**：Windows 11｜Python 3.13 venv｜torch 2.14.0 + cpu｜von-sdk 1.2.2｜（另需 Python 3.14 的 `compression.zstd` 用于解压日志）

**模型权重**：`NanoJev`（2,385,039,280 B）｜`von-1.0` 版本 1.2（3,021.90 MB；CDN `X-Linked-Size/ETag` 校验 SAME；**精确 revision 未记录** —— 见 §7 局限 6）

**数据**（均为公开数据集，`validation` 划分）：

```
cmnli_val.parquet           1,354,747 B   CLUE → cmnli/validation
mnli_val_matched.parquet    4,938,568 B   MultiNLI → validation_matched
boolq_val.parquet           1,257,630 B   BoolQ → validation
xnli_zh_val.parquet           157,413 B   XNLI → zh/validation
xnli_en_val.parquet           157,207 B   XNLI → en/validation
ocnli_val.parquet             153,558 B   OCNLI（二分类版，未采用为主集）
```

**脚本（自建探针，未随文公开；文件名为对应各实验）**：自有域评测与 criteria 对照｜输入敏感性＋标签互换｜原生域对照｜中文公开集｜英文原版｜2×2 交叉｜XNLI 配对（含逐题分数落 JSON）｜12 题换英文 criteria。

**★ 脚本模板前置检查（本次固化）**：①**金标样本断言**（把数据集官方文档示例硬编码进脚本，加载后 `assert` 标签/列名匹配）；②**冒烟 2 条**（全量前打印 state ＋ criteria ＋ label 供人工过目）；③**标签分布表**（采样前打印）；④**表头由配置生成**（禁派生模板硬编码文案）。

**环境坑（均为实测）**：Hugging Face 主站不可达 ⇒ 须走镜像端点；某镜像 pip 源对 `transformers` 返回空集 ⇒ 换源；大仓库 `git clone` 超时 ⇒ 走 `snapshot_download`；Windows 下部分 Python 工具不认 `/c/...` 形式路径。

---

## 9 AI 使用声明、作者贡献与诚信声明

**AI 参与披露（本节不可省略）**：

1. **本文第一作者为一个 AI 智能体**，负责全部实验设计执行、数据处理、统计计算与撰写（**不是人类研究者**）；
2. **另有一个由不同厂商提供的通用大语言模型系统担任独立复核方**。其工作方式是：**只接收执行方提供的文本与数据**，独立给出审阅意见，**不接触执行方的原始文件**。**该复核方是 AI 系统，不是人类专家**；本文**任何时候都不得将其表述为「独立专家裁定」**；
3. **一名人类决策者**参与方向裁定与授权，**不参与**数据采集、统计分析与文字撰写；
4. 全文所有数值均由执行方的实测脚本产出，**外部结论凡未独立复现者均已在文中逐条标注「外部·未复现」**；
5. **作者资格前置项**：多数会议／期刊的作者政策**不接受 AI 系统作为署名作者**。投稿前必须按目标场所政策落实——或由人类研究者作为署名作者并如实披露 AI 贡献，或改投接受非人类作者的场所；**本项未落实前不得投稿**。

**作者贡献**：实验设计、实测执行、数据分析、撰写——执行方（AI 智能体）；独立复核——复核方（另一 AI 系统）；方向裁定与授权——人类决策者。

**诚信声明**：

1. 本文全部数字**可溯源**至脚本输出、原始 JSON 或落盘回件；
2. 所有**外部宣称**均标注「未复现」；
3. 本文**含未闭合问题 6 项**（附录 C·C.3），**不因发布而消失**；
4. 本文的**最强结论**是「`state` 语言效应大且经配对正式检验」；**最弱结论**是「自有域未检出判别力」（n = 12，检验力不足）；
5. 本文**保留自身错误的完整台账**（附录 C），包括 12 项已撤回的判断。

---

## 参考文献

1. Clark, C., Lee, K., Chang, M.-W., Kwiatkowski, T., Collins, M., & Toutanova, K. (2019). *BoolQ: Exploring the Surprising Difficulty of Natural Yes/No Questions.* In Proceedings of NAACL-HLT 2019. arXiv:1905.10044.
2. Williams, A., Nangia, N., & Bowman, S. R. (2018). *A Broad-Coverage Challenge Corpus for Sentence Understanding through Inference.* In Proceedings of NAACL-HLT 2018, pp. 1112–1122. DOI: 10.18653/v1/N18-1101. arXiv:1704.05426.
3. Conneau, A., Rinott, R., Lample, G., Williams, A., Bowman, S. R., Schwenk, H., & Stoyanov, V. (2018). *XNLI: Evaluating Cross-lingual Sentence Representations.* In Proceedings of EMNLP 2018, pp. 2475–2485. DOI: 10.18653/v1/D18-1269.
4. Hu, H., Richardson, K., Xu, L., Li, L., Kübler, S., & Moss, L. (2020). *OCNLI: Original Chinese Natural Language Inference.* In Findings of EMNLP 2020, pp. 3512–3526. DOI: 10.18653/v1/2020.findings-emnlp.314. arXiv:2010.05444.
5. Xu, L., Hu, H., Zhang, X., et al. (2020). *CLUE: A Chinese Language Understanding Evaluation Benchmark.* In Proceedings of COLING 2020, pp. 4762–4772. DOI: 10.18653/v1/2020.coling-main.419.
6. Guo, C., Pleiss, G., Sun, Y., & Weinberger, K. Q. (2017). *On Calibration of Modern Neural Networks.* In Proceedings of the 34th International Conference on Machine Learning (ICML), PMLR 70:1321–1330.

---

## 附录 A · 数据集与许可（合规登记）

| 数据集 | 在本文中的用途 | 许可条款 | 来源与核实方式 |
|---|---|---|---|
| **CLUE → CMNLI** | 中文公开集主集 | **数据集卡片标注 `license: unknown`（未标注许可）** ⚠️ | 数据集卡片 YAML 元数据实测读取（`clue/clue`，许可字段值 `unknown`）；复用前须另行确认 |
| **MultiNLI** | 英文原版对照 | 卡片标注**多许可**：`cc-by-3.0`／`cc-by-sa-3.0`／`mit`／`other`；`license_details` 为「Open Portion of the American National Corpus」；原始发布页注明「许可详见数据描述论文」 | 数据集卡片元数据实测读取；原始发布页 |
| **BoolQ** | 英文原生域对照（PC-1） | **CC BY-SA 3.0** | 数据集卡片元数据实测读取（`google/boolq`，`license: cc-by-sa-3.0`） |
| **CLINC150** | 域外拒识（闸门段，§4.3） | 本文所用镜像 `contemmcm/clinc150` **未标注许可** ⚠️；**上游官方 `clinc/clinc_oos` 为 CC BY 3.0** | 两处数据集卡片元数据实测读取（镜像无 `license` 字段；官方卡片 `license: cc-by-3.0`） |
| **XNLI** | 逐题配对的跨语言材料 | **CC BY-NC 4.0（署名—非商业性使用）** ⚠️ **非商业限制** | 官方任务页与官方发布说明；数据集卡片未含许可字段 |
| **OCNLI** | 备选（未采用） | 官方数据页标注 **CC BY-NC 2.0** ⚠️ 非商业（**待以官方源复核**） | 官方数据托管页公开标注；**未取原始许可文件 ⇒ 待核** |
| 自造中文短规则命题 | 自有域 | 本文自建，不含第三方内容 | — |

> **合规提示**：XNLI 与 OCNLI 均为**非商业**许可；**CLUE（CMNLI）与 CLINC150 镜像许可未标注**（CLINC150 上游官方为 CC BY 3.0）。**任何商业用途的复用须另行取得授权**。本文仅作学术评测使用。

## 附录 B · 参照的外部公开来源清单（按主题）

> **说明**：本清单列出本文**实际打开或引用过**的全部外部公开来源；标「未复现」者为本文**未做独立验证**。**本清单的完整性受限于本次检索面**（GitHub／Hacker News／全网中英文），**未覆盖** Reddit 原帖逐条与 arXiv 正式检索。

**B.1 独立基准与评测（10 项）**

| # | 来源 | 日期 | 用途 |
|---|---|---|---|
| 1 | `decision-model-benchmark`（DMB） | 09-23 | **核心对照**：官方模型的 ECE 0.246、forced-uncertainty 49.7%、255 上限、位置偏置 13%；§2.1／§5.2 引用（未复现） |
| 2 | `jev-baselines-eval` | 09-23 | 预注册 ＋ CI ＋ kill/go 判据的**规范模板**；CLINC150 结论；§2.1／§3.2 引用（未复现） |
| 3 | `BioJev` | 09-23 | 跨数据集泛化实验的**方法参照**（未复现） |
| 4 | 一个中文作者的 `jev-eval` | 09-22 | 「可用阈值」一环促成本文 §7 局限 2（未复现） |
| 5 | `finnhll/jev-eval` | 09-20 | 282 条记录离线可复现 ⇒ **可复现性参照**（未复现） |
| 6 | `dshvimer/jev-eval` | 09-19 | 分类基准，与 LLM 对比（未复现） |
| 7 | `dchristopoulos/jev-aita` | 09-2x | **Brier score ＋ CI ＋ prompt 格式对比**（未复现） |
| 8 | `apolinario/decision-index` | 09-23 | 基准套件复现工具（未复现） |
| 9 | `4esv`／`themsquared`／`classmethod` 等 | 09-17 ～ | 同期独立评测（≥16 个变体，未复现） |
| 10 | `chr-kelly/jev-cookbook` | 09-20 | 提供 CLINC150 评测与 harness（未复现） |

**B.2 同族模型测量与校准方法学（4 项）**

| # | 来源 | 用途（均未复现） |
|---|---|---|
| 11 | SOTAAZ《Jev in 10 Minutes》 | 同族实现域外 TREC 86.6%／BANKING77 37%（候选挤压，有解）⇒ 反证「域外必坍缩」 |
| 12 | `fawadhs.dev`《How Jev Works, and How to Test a Decision Model》 | 「0.5 是约定不是模型属性」、拟合阈值改变结论、欠自信（温度 0.47–0.83）、TF-IDF ＋ LR 基线 |
| 13 | `dev.to` 一篇实现综述 | 「无人复现 RLCD」；von 自带 calibration file ＋ 重拟合脚本 |
| 14 | `MindStudio`（Minecraft 实测） | 行为层「重复/坍缩」最接近的外部观察 |

**B.3 官方与社区（4 项）**

| # | 来源 | 用途 |
|---|---|---|
| 15 | 该类模型官方文档与「参差性」页 | 已知缺陷清单（含**非英语准确率较低**） |
| 16 | Hacker News 官方发布帖 ／ Reddit r/LocalLLaMA | 生态热度与先行工作事实（**本文以 HN 官方 API 一手实测分数**） |
| 17 | 中文社区：51CTO／腾讯新闻／掘金／一篇技术博客《System One》 | 中文侧舆图与争议点 |
| 18 | 9 份 `awesome` 型清单 | 生态规模量化 |

**B.4 学术脉络（3 项 · **本文初稿遗漏，且出处未核实**）**

| # | 来源 | 用途 |
|---|---|---|
| 19 | Springer 论文《Explanation audits reveal silent failures of machine learning models under distribution shift》（2026-08-24） | 「静默失败」的正式定义与量化方法（**出处未核实**） |
| 20 | MIT／NeurIPS 2025 一项研究（推翻「Accuracy-on-the-Line」） | 高平均准确率掩盖子群失败（**出处未核实**） |
| 21 | 《Is Capability a Liability?》（arXiv:2605.22672，2026-05） | **逆缩放**：能力越强在 regime change 处越差（**出处未核实**） |

**B.5 验证方法论与合规（2 项）**

| # | 来源 | 用途 |
|---|---|---|
| 22 | 一个第三方验证协议（逐帧重放轨迹） | 「决策模型的验证单元是整个轨迹」；其 CRLF 教训与本文同型 |
| 23 | 一份以我国主权议题为题的立场测试件 | **按合规要求不予引用/展开**（§2.8） |

## 附录 C · 已修正判断与已否证假设台账（**保留，不粉饰**）

> 本文的价值之一在于：**把错误连同证伪过程一并留下**，作为同类判断的校准锚点。以下按「已固化为护栏／已被自己否证的怀疑／仍未闭合」分列。

### C.1 已被固化为护栏的错误

| # | 错误 | 证伪过程 | 固化护栏 |
|---|---|---|---|
| 1 | 把「高概率」当成「有判断力」（曾判「boolean 题型更强、应从 boolean 切入」） | 做 12 题平衡样本后，预测 True 12/12、准确率恰等于基线 | **三条护栏**（正负平衡 ＋ 基线对照 ＋ 报分布） |
| 2 | 把单个模型的失败上升为路线的失败（曾判「现成 checkpoint 路线到此为止」） | 在低分帖中挖到另一实现与另一数据集 | **单模型失败 ≠ 路线失败**；下路线级结论前须穷尽检索（≥四路） |
| 3 | 初稿两处表述性错误：①顺序扰动「翻转 0/12」被当成「order-invariance 兑现」（**零信息实验**）②同一 AUC 同时用于「命中否证」与「方向反向」（**自相矛盾**） | 复核方逐条指出；回算 SE ≈ 0.075、CI ≈ [0.32, 0.62] 含 0.5 | **零信息实验三问**（§5.4）＋ 同一指标禁两用于互斥结论 |
| 4 | 把**结构性恒等式**当行为证据（PC-3 的 `A + B = 1.0000`） | 复核方：二分类 softmax 下互换**必然**互补 | 同上（**恒等式型**） |
| 5 | **研究成果产出未做外部查新** ⇒ 贡献主张重复、两处结论失准 | 外部提示后触发普查，发现同类工作远超本文 | **研究产出交付前必做外部查新（≥3 个检索面）＋ 检索须覆盖衍生名** |
| 6 | **前向数算错**（早期记载 1,092，实际 2,640） | 逐项以实验清单重算 | **总数须逐项列举并求和**，不得凭印象给总数 |

### C.2 被自己实验否证的怀疑（**记录以示方法**）

| # | 本文的怀疑 | 自证伪结果 |
|---|---|---|
| 1 | 「该实现的校准未被应用」 | 脚本改造前的对照实跑：**不传 criteria 结果同向** ⇒ 怀疑不成立 |
| 2 | 「接口配置（criteria）导致假阴性／接口用反」 | 同上 ⇒ 排除（另见 §4.4 的结构性恒等式说明） |
| 3 | 「中文 criteria 措辞是自有域低分的原因」 | **12 题换英文 criteria 后准确率与预测标签 0 变化**（§4.10；`noul` 均值 0.3175→0.2350，分数有移动但未改变判定）⇒ 怀疑不成立 |

### C.3 至今**未闭合**的问题（**保留在册**）

| # | 未闭合项 | 影响 |
|---|---|---|
| 1 | **未做阈值拟合** | 「预测单侧」可能是阈值不当的产物（**未排除**） |
| 2 | **CMNLI vs MultiNLI 未做 ΔAUC 两样本正式检验** | 该对比的「明显高于」仅依据 CI 不重叠（保守判据） |
| 3 | **BoolQ 训练污染未查** | 82.0% 不得作泛化能力证据 |
| 4 | **12 题样本检验力不足** | 「未检出」≠「不存在」 |
| 5 | **权重 revision 未锁定**；12 题题面／prompt 模板原文未留档 | 第三方不能完整复现 |
| 6 | **`criteria` 与 `instructions` 捆绑，未分离**；XNLI 仍为翻译集 | 「语言」与「措辞／译本风格」不可完全分离 |

### C.4 已**撤回**的判断（12 项 —— 换环境重做同类研究时最易重复踩坑处）

| # | 已撤回的判断 | 撤回理由 |
|---|---|---|
| 1 | 「boolean 题型更强，应从 boolean 切入」 | **证伪**：那是先验偏 True 的常数输出 |
| 2 | 「现成 checkpoint 路线到此为止」 | **证伪**：未穷尽开源实现检索 |
| 3 | 「退化为**恒答常数分类器**」 | **措辞撤回**：12 个 `noul` 值**互不相同**；真实形态 ＝「**分布窄 ＋ 正负区间重叠 ＋ 预测标签单侧**」 |
| 4 | 「该话题在 Hacker News 未被接住（分数 1–5）」 | **错**：实有 459 分/200 评等条目；错因 ＝ **检索词过窄** |
| 5 | 「高置信输出不携带信息」＝本文**独立发现** | **降级为复现**：外部 DMB 已实测官方模型 ECE 0.246、forced-uncertainty 仅承认 49.7% |
| 6 | 「PC-3 的 `A + B = 1.0000` 可排除接口用反」 | **撤回**：**结构性恒等式**、零信息 |
| 7 | 「差异来源：题面形态／出题质量／**样本量**」 | **撤回**：**样本量不是「效应来源」**（n 只影响 CI，不产生点估计的期望差）；且 **n = 12 的 CI ≈ ±0.28 ⇒ 包含 CMNLI 的 0.668** ⇒「两域不同」不成立 |
| 8 | 「中文 criteria 是损害项**、可解释 12 题低分**」 | **部分撤回**（v2 缩窄）：撤「可解释 12 题低分」（§4.10 实测：自有域换英文 criteria 后准确率与预测标签 0 变化）；**保留「在中文 `state` 的 NLI 形态下 `criteria` 语言是损害项」**（§4.9：+0.1654，显著）——v1 整条撤回与此自相矛盾，已更正 |
| 9 | 「前向数 1,092」 | **算不平**，正确 **2,640**（§4.11） |
| 10 | 「复核方读不到待评文件 ＝ 其文件工具受工作目录约束」 | **未确证**（复核方后称相关路径均可读取） |
| 11 | 「边际差 0.2283 ＋ 0.0741 ≈ 原总差 ⇒ 近似可加」 | **否证**：这是**边际分解的近似恒等式**；且与**交互项 DiD ≈ −0.127** 矛盾 |
| 12 | 「CLINC150 的 AUC 0.4700 **低于随机、方向反向**」 | **撤回**：95% CI 含 0.5 ⇒ 只能写「**与随机不可区分**」 |

## 附录 D · 术语与缩略语

| 术语 | 含义 |
|---|---|
| **小决策模型 / System One 模型** | 不生成文本，只接收「状态 ＋ 类型化问题」并返回**带概率的类型化决策**的一类模型；三类原语为 `Noul`／`Choice`／`Score` |
| **`Noul`** | 返回 *P*(yes) 的布尔决策原语 |
| **`state`** | 模型的输入状态（本文中为待判定的事实陈述或句对） |
| **`criteria`** | 标签侧的描述文本（如「该说法正确／错误」），用于自定义选项语义 |
| **`instructions`** | 任务指令文本（如「请判断该说法是否成立。」） |
| **AUC** | 受试者工作特征曲线下面积；本文用于衡量二值化后的判别力 |
| **组差** | 正样本 `noul` 均值 − 负样本 `noul` 均值 |
| **ΔAUC（配对）** | 同批 index 逐题配对下两配置的 AUC 之差，配 bootstrap 95% CI |
| **ECE** | 期望校准误差（Expected Calibration Error） |
| **not_collapse／collapse／灰区** | 见 §3.5 的机器可判判据 |
| **零信息实验** | 即使被测对象**完全失效**也照样成立的实验（§5.4 三形态） |

---

**版本轨迹**：v1（初稿，审核对象）→ 复核修正（12 项）→ 外部查新 → 对照实验 4 组 → v3 内部全量版 → 复核审核 → v1 对外发表版 → **对外版独立复核（评级 major revision）→ v2 对外发表版（本版）**

**v2 修订依据与清单**：对外发表版的独立复核回件（正式答复 3,092 字 ＋ 推理草稿 21,850 字，原件存于项目档案）。共修补 24 处，覆盖：**统计补证**（交互项 DiD 正式检验 ＋ 多重比较声明，零新增前向）｜**客观事实与计数**（§1.3「八组」→十组、摘要数据集数、§3.4 XNLI 行数注、附录 A 补 CLINC150 许可、OCNLI 许可待核、合规提示）｜**强度与口径**（§4.10「0 变化」与「两域机制不同」标注、RQ2 措辞、术语统一、C.2#3／C.4#8 措辞）｜**投稿前置**（§9 作者资格）｜**版本标注**。**回核后不采纳 2 条**：复核方称「XNLI 2,490 行待验证」「`bias > 0.6` 出处无从核」——经回核，前者 HF 卡片实测即 2,490，后者出处见独立的对照实验记录（存于项目档案）§四。
